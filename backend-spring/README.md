# backend-spring

## Requirements

- JDK 25 (latest LTS)
- Gradle 9+ (or Maven if you convert the build)

## Run

```bash
gradle bootRun
```

Server starts on `http://localhost:5010`.

## Endpoints

- `GET /`
- `GET /health`
